#include <iostream>
#include <math.h>
using namespace std;

int main(int argc, char **argv){
    int a,b,c;
    cin >> a >> b >> c;
    int a1= pow(a,2);
    int b1= pow(b,2);
    int c1= pow(c,2);
    if(a1 = (b1 + c1))
        cout << "true";
    else
        cout << "false";
}